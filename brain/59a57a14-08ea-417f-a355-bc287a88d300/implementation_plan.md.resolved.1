# Fix TypeScript Error in OverrideList.tsx

Rename the incorrectly named prop [setRowSelection](file:///c:/SCOUTNEW/scout_db/frontend/src/components/shared/DataTable.tsx#52-63) to `onRowSelectionChange` when using the [DataTable](file:///c:/SCOUTNEW/scout_db/frontend/src/components/shared/DataTable.tsx#38-222) component.

## Proposed Changes

### scout_db/frontend/src/pages/Overrides

#### [MODIFY] [OverrideList.tsx](file:///c:/SCOUTNEW/scout_db/frontend/src/pages/Overrides/OverrideList.tsx)

- Rename `setRowSelection={setRowSelection}` to `onRowSelectionChange={setRowSelection}` on the [DataTable](file:///c:/SCOUTNEW/scout_db/frontend/src/components/shared/DataTable.tsx#38-222) component.
- Add explicit type to `useState` for `rowSelection`: `useState<Record<string, boolean>>({})`.
- Add explicit type to `useQuery` to define the response structure: `useQuery<{ items: any[], total: number }>(...)`.
- Use optional chaining or safe access for `data.items` in the `selectedIds` memo.

## Verification Plan

### Automated Tests
- Run `npm run build` or `npx tsc` in the frontend directory to verify that the TypeScript error is gone.
- Since `npm run dev` is already running, check the terminal output for success.

### Manual Verification
- Verify that row selection still works in the Overrides list.
- Verify that the "Delete Selected" button appears correctly when rows are selected.
